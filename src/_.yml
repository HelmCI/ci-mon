kind: all
store:
  db:
    postgres: &db pass
  secrets: # by NS: need move to .env.yml
    homer7:
      DB_PASS: *db
    homer:
      ADMIN_PASSWORD: admin
    demo-wp:
      MYSQL_PASSWORD: examplepass
